#include "libft.h"
#include <string.h>
#include <ctype.h>
#include <stdio.h>

int	main()
{
	const char	*string = NULL;

	char	copia1[15];
	char	copia2[15];
	// ft_memcpy(0, 0, 10);
	memcpy(0, 0, 10);

	// printf("%s\n", copia1);
	// printf("%s\n", copia2);
	return (0);
}
